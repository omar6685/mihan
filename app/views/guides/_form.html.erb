<%= form_with(url: generate_report_guides_path, method: :post, local: true, multipart: true) do |form| %>
  <!-- Display errors if any -->
  <% if guide.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(guide.errors.count, "error") %> prohibited this guide from being saved:</h2>

      <ul>
        <% guide.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>  
    <!-- File upload field for .xlsx file -->
    <%= form.file_field :xlsx_file, accept: '.xlsx' %>

    <!-- Submit button -->
    <%= form.submit "Generate Report" %>
  </div>
<% end %>
